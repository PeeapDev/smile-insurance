# --- Next.js + Supabase environment variables ---
# Copy this file to `.env.local` and fill in the values.

# Public URL of your Supabase project (found in Supabase Dashboard > Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=

# Public anon key (safe to expose on the client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key (server-side only, DO NOT expose to the browser)
# Add only to Vercel Project Settings (Environment Variables) or a local server-only env
SUPABASE_SERVICE_ROLE_KEY=

# Optional: Node environment
NODE_ENV=development

# Next.js (optional) - set if you want a non-default port locally
# PORT=3000

# --- Monime Payments (server-side) ---
# Required: API key (test: mon_test_..., live: mon_...)
MONIME_API_KEY=
# Required: Space ID from Monime dashboard
MONIME_SPACE_ID=
# Optional: Lock to a specific API version (e.g., caph.2025-08-23)
MONIME_API_VERSION=
# Optional: Override API base (default https://api.monime.io)
MONIME_API_BASE=
# Required for verifying webhooks (configure in Monime and here)
MONIME_WEBHOOK_SECRET=
